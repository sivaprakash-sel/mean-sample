import { EventEmitter } from '@angular/core';
import { Ng2SortableService } from '../services/ng2-drag-drop.service';
export declare class SortableContainer {
    private sortableService;
    /**
     * The items that can be sorted
     */
    sortableItems: Array<any>;
    /**
     * Delete items on swap
     */
    deleteOnSwap: boolean;
    /**
     * Event fired when item is dragged to another list
     */
    onSwap: EventEmitter<any>;
    constructor(sortableService: Ng2SortableService);
    dragLeave(e: any): void;
    dragOver(e: any): void;
    drop(e: any): void;
    dragEnter(e: any): void;
}
